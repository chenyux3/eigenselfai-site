---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog'))
	.filter((post) => !post.data.draft)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.intro {
				margin: 0 0 4em;
			}
			ul {
				list-style: none;
				margin: 0;
				padding: 0;
			}
			li {
				margin: 0 0 0.25em;
			}
			li a {
				color: var(--text-strong);
				text-decoration: none;
				display: block;
				font-size: 1.0625em;
				font-weight: 500;
				line-height: 1.4;
				padding: 0.5em 0.75em;
				margin: 0 -0.75em;
				border-radius: 4px;
				transition: background-color 0.15s ease;
			}
			li a:hover {
				background-color: rgba(255, 255, 255, 0.08);
			}
			li a .arrow-svg {
				margin-left: 0.45em;
				vertical-align: middle;
				color: var(--text-muted);
				transition: color 0.15s ease;
			}
			li a:hover .arrow-svg {
				color: var(--text-strong);
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro" lang="zh-Hans">
				<p>
					EigenSelf 是一个自我理解框架，帮你在标签和效率之外，看见自己的内部价值结构。它不告诉你"你是哪一种人"，也不给你贴新标签，而是关注基本价值在你心里如何连接、冲突、支撑和激活彼此。
				</p>
				<p>
					通过一份简短的价值排序与联想调查，它会生成一份结构化报告：什么真正牵动你，什么让你反复卡住，什么在支撑你的选择，什么是你下一步需要认真面对的问题。
				</p>
				<p>
					EigenSelf 不替你做决定，而是帮你更清楚地理解自己，从而更有力量地处理真实生活里的问题。
				</p>
				<p>
					如果你对 EigenSelf 感兴趣，这里有一份 3 分钟的 <a href="https://chenyux3.github.io/eigenself-survey/" target="_blank" rel="noopener">小测试</a>，也欢迎联系我：<a href="mailto:info@eigenself.ai">info@eigenself.ai</a>。
				</p>
				<p>
					想了解更多，可以读读下面的文章。
				</p>
			</section>
			<section>
				<ul>
					{posts.map((post) => (
						<li>
							<a href={`/blog/${post.id}/`}>
								{post.data.title}
								<svg
									class="arrow-svg"
									width="16"
									height="10"
									viewBox="0 0 16 10"
									fill="none"
									stroke="currentColor"
									stroke-width="1"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
								>
									<line x1="1" y1="5" x2="14" y2="5" />
									<polyline points="10,1.5 14,5 10,8.5" />
								</svg>
							</a>
						</li>
					))}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
