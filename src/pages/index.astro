---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog'))
	.filter((post) => !post.data.draft)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.intro {
				margin: 0 0 4em;
			}
			ul {
				list-style: none;
				margin: 0;
				padding: 0;
			}
			li {
				margin: 0 0 1.25em;
			}
			li a {
				color: var(--text-strong);
				text-decoration: none;
				display: block;
				font-size: 1.0625em;
				font-weight: 500;
				line-height: 1.4;
			}
			li a:hover {
				text-decoration: underline;
				text-underline-offset: 3px;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro" lang="zh-Hans">
				<p>
					EigenSelf 是一个自我理解框架，帮助普通人在标签和效率之外，看见自己的内部价值结构。
				</p>
				<p>
					它不试图告诉你"你是哪一种人"，也不再给你贴一个新标签。它关注的是：基本价值在你心里如何连接、冲突、支撑和激活彼此。
				</p>
				<p>
					通过一份简短的价值排序与联想调查，EigenSelf 会形成一份结构化报告，帮助你看见自己的价值网络：什么真正牵动你，什么让你反复卡住，什么在支撑你的选择，什么可能是你下一步需要认真面对的问题。
				</p>
				<p>
					EigenSelf 不替你做决定。
				</p>
				<p>
					它帮助你更清楚地理解自己，从而更有力量地处理真实生活里的问题。
				</p>
				<p>
					如果你对 EigenSelf 感兴趣，欢迎联系我：<a href="mailto:info@eigenself.ai">info@eigenself.ai</a>
				</p>
			</section>
			<section>
				<ul>
					{posts.map((post) => (
						<li>
							<a href={`/blog/${post.id}/`}>{post.data.title}</a>
						</li>
					))}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
